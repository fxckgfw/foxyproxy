* rermoved Flock support as it is not actively developed anymore (see: http://flock.com/) 
* fix bug in handling regular expressions ("^" and "$" was always added to
  single line regular expressions)
* removed height/width persistence in dialogs where sizeToContent() is
  called solving the issue of growing dialogs finally
