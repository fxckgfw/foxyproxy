* included auto-detect of network settings (WPAD feature)

* added a cancel button to addeditproxy dialog
* show SOCKS version in proxy tree only if a SOCKS proxy is selected in the
  proxy settings
* show enable SOCKS version radio buttons only if a SOCKS proxy is selected at
  all
* Removed OCS from about window
* Closed help popups in the add/edit pattern subscription window when clicking
  one of the links in the help popups